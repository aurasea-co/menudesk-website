node_modules/
.next/
out/
build/
.env
.env.local
.env.*.local
.DS_Store
*.log
.vercel
next-env.d.ts

# Internal working folders
design/
content/
.env*
# ...but the template must be committed: it is how the next person learns
# which variables the site needs. `.env*` would swallow it silently.
!.env.example

# tsc --noEmit incremental cache
*.tsbuildinfo
